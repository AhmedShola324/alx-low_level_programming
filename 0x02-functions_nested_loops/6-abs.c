#include "main.h"
/**
 * _abs- returns the absolute value of an integer
 * @n: takes an integer as an arguement
 * Return: Absolute value of the argument
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}



